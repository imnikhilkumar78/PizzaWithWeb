@model PizzaApplication.Models.PizzaDTO

@{
    ViewData["Title"] = "Select_This";
}

<div class="card">
    <div class="card-header">
        <P style="color: darkolivegreen"> Pizza Selected </P>
        <p></p>
    </div>
    <div class="card-body">
        <div class="btn btn-dark">
            @Html.ActionLink("Go to Checkout", "Checkout")
        </div>
        <hr />
        <div class="btn btn-dark">
            @Html.ActionLink("Add Another Pizza", "AddPizza")
        </div>
        <hr />
        <div class="btn btn-dark">
            @Html.ActionLink("Add Toppings", "AddTopping")
        </div>

    </div>
</div>